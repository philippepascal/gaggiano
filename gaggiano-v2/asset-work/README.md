Source images for the UI. Only `logoMainScreen.png` is currently compiled in (as
`../logoMainScreen.c`, generated with the LVGL image converter v8,
https://lvgl.io/tools/imageconverter, RGB565). The others are kept for future use.
